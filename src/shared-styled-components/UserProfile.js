import styled from "styled-components";

const UserProfile = styled.img`
  height: 24px;
  width: 24px;
  border-radius: 50%;
  cursor: pointer;
  margin-left: auto;
  margin-right: 0;
`;

export default UserProfile;
