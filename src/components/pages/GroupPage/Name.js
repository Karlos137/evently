import styled from "styled-components";

const Name = styled.h1`
  font-size: ${props => props.theme.fontSizes.large};
  width: 311px;
  margin: 32px auto;
  text-align: center;
`;

export default Name;
